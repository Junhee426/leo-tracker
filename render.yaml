services:
  - type: web
    name: global-leo-tracker
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.lock
    startCommand: gunicorn app:app
    healthCheckPath: /health
    autoDeploy: true
